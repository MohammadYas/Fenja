# python (toolkit scripts)
__pycache__/
*.pyc
.pytest_cache/

# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# testing
/coverage

# env — secrets må ALDRIG committes (HANDOFF §5.2); kun .env.example er i git
.env
.env.*
!.env.example

# misc
.DS_Store
*.tsbuildinfo

# supabase lokal state
/supabase/.branches
/supabase/.temp

# lokale verifikations-screenshots og harness (aldrig committes)
/.verify/
/.worktrees/

# lighthouse ci-rapporter
/.lighthouseci/

# markedsanalyse-output (øjebliksbilleder, aldrig kildekode)
/data/markedsanalyse/

# gate 1-fotos og -resultater (ejerens egne billeder, aldrig committes)
/data/gate1/

# impeccable live session state (runtime token/pids, aldrig committes)
/.impeccable/live/

# Local Netlify folder
.netlify

# Netlify edge-bundling lagger denne under deploy
deno.lock

# Lokale testkørsler (model-tvekamp + Gate 1) — billeder og ark, aldrig i git
tvekamp-ud/
gate1-rapport.html
